[build]
  command       = "npm run build"
  publish       = "dist"

[build.environment]
  NODE_VERSION  = "18"

# SPA catch-all — send every route to index.html so React Router works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
